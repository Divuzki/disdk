{
  "name": "@disdk/charge",
  "version": "0.1.0",
  "private": true,
  "description": "Pull payments: the service that holds the delegate key and charges an approved wallet under configured terms",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch --experimental-strip-types src/main.ts",
    "start": "node --env-file-if-exists=.env --experimental-strip-types src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@disdk/protocol": "workspace:*",
    "@disdk/verify": "workspace:*",
    "@hono/node-server": "^1.14.0",
    "@solana/kit": "^7.1.0",
    "hono": "^4.13.2"
  },
  "devDependencies": {
    "@solana-program/token": "^0.15.0",
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
